% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/med_mod_code.R
\name{run_moderated_mediation}
\alias{run_moderated_mediation}
\title{Conduct first-stage (and, optionally, direct-path) moderated mediation analysis}
\usage{
run_moderated_mediation(
  data,
  X,
  M,
  W,
  Y,
  CVs = NULL,
  mod_direct_path = TRUE,
  standardized_all = TRUE,
  conf_level = 0.95,
  seed = NULL,
  bootstraps = 5000,
  cores = 1,
  ...
)
}
\arguments{
\item{data}{Data frame}

\item{X}{Predictor variable (all variables should be passed 'bare' in tidyverse style)}

\item{M}{Mediator variable}

\item{W}{Moderator variable (assumed continuous, centered/standardized internally)}

\item{Y}{Outcome variable}

\item{CVs}{Covariates (in predicting \code{M} \emph{and} \code{Y})}

\item{mod_direct_path}{Logical. Should the direct path from \code{X} to \code{Y} also be
moderated by \code{W}? Defaults to \code{TRUE}. If \code{FALSE}, only the first-stage (\code{X} to \code{M})
path is moderated.}

\item{standardized_all}{Logical. Should all variables (\code{X}, \code{M}, \code{W}, \code{Y} and any \code{CVs})
be standardized (z-scored) before the product term is formed and the model is estimated?
If \code{FALSE}, only \code{X} and \code{W} are mean-centered before the product term is formed - this
is the minimum needed for interpretable conditional effects, but paths remain in the
original (unstandardized) metric of \code{M}, \code{Y} and any \code{CVs}.}

\item{conf_level}{The confidence level to be used for confidence intervals. Must be between 0 and 1
(exclusive), defaults to .95 (i.e. 95\% confidence intervals).}

\item{seed}{Random seed. You should set this to get reproducible results. If \code{NULL} (default),
the seed is not set by this function, and results will thus vary between runs.}

\item{bootstraps}{Number of bootstraps, defaults to 5000.}

\item{cores}{Number of CPU cores to use for bootstrapping. Defaults to 1
(sequential processing); set higher to speed up bootstrapping on multi-core machines.}

\item{...}{Options passed on to \code{\link[lavaan:sem]{lavaan::sem()}}.}
}
\value{
A tibble (with S3 class \code{c("timesaveR_mod_med", class(tibble))}) with one
row per parameter, \code{type} being one of:
\describe{
\item{a}{X -> M path}
\item{a_mod}{Moderation of the X -> M path by W (i.e., the X:W product term predicting M)}
\item{w_m}{W -> M path}
\item{b}{M -> Y path}
\item{c}{X -> Y direct path (at the mean of W, since W is centered)}
\item{c_mod}{Moderation of the X -> Y path by W (only if \code{mod_direct_path = TRUE})}
\item{w_y}{W -> Y path}
\item{imm}{Index of moderated mediation (\code{a_mod * b})}
\item{ind_low}{Conditional indirect effect of X on Y through M, at W = mean - 1 SD}
\item{ind_mean}{Conditional indirect effect of X on Y through M, at the mean of W}
\item{ind_high}{Conditional indirect effect of X on Y through M, at W = mean + 1 SD}
\item{dir_low}{Conditional direct effect of X on Y, at W = mean - 1 SD (only if \code{mod_direct_path = TRUE})}
\item{dir_mean}{Conditional direct effect of X on Y, at the mean of W (only if \code{mod_direct_path = TRUE})}
\item{dir_high}{Conditional direct effect of X on Y, at W = mean + 1 SD (only if \code{mod_direct_path = TRUE})}
}
Further columns are \code{est} and \code{se} (mean and SD of the bootstrap draws), \code{pvalue}
(for the raw lavaan parameters - \code{a}, \code{a_mod}, \code{w_m}, \code{b}, \code{c}, \code{c_mod}, \code{w_y} - this is
the Wald-test p-value returned by \code{lavaan}; for the derived quantities - \code{imm} and the
conditional indirect/direct effects - this is a bootstrap-based two-sided p-value,
\code{2 * min(mean(draws <= 0), mean(draws >= 0))}, since no analytical p-value is available
for them), and \code{ci.lower}/\code{ci.upper} (bootstrap percentile confidence intervals for all
parameters). Coefficients for covariates are returned as the \code{CV_coefficients} attribute,
and the \code{lavaan} model code is returned as the \code{lavaan_code} attribute - see the example.
}
\description{
Runs a moderated mediation analysis with a single mediator (using the \code{lavaan}
package): the path from \code{X} to \code{M} (the \emph{a}-path) is moderated by \code{W}, and,
optionally, so is the direct path from \code{X} to \code{Y} (the \emph{c}-path). The path from
\code{M} to \code{Y} (the \emph{b}-path) is not moderated. This is exactly the model drawn by
\code{\link[=plot_moderated_mediation]{plot_moderated_mediation()}}, and the result of this function can be passed
straight to \code{plot_moderated_mediation(data = ...)} to create an annotated
diagram of the results.
}
\details{
\code{W} is assumed to be continuous and is mean-centered (or standardized, see
\code{standardized_all}) internally before the product term \code{X:W} is formed - this is
standard practice to keep the lower-order coefficients (\code{a}, \code{c}) interpretable
as conditional effects at the mean of the moderator, and to reduce (non-essential)
multicollinearity between the product term and its components.

Note that this function only supports a single mediator, moderated first-stage and
(optionally) direct path - i.e. it implements the model most commonly described as
"moderated mediation" in introductory treatments (e.g. Hayes' PROCESS model 7 or 8).
For (unmoderated) mediation with one or more parallel mediators, see \code{\link[=run_mediation]{run_mediation()}}.

As with \code{run_mediation()}, covariates (if given) are used in predicting both \code{M} and
\code{Y}, and their coefficients are returned as an attribute - see \code{attr(res, "CV_coefficients")}.
The lavaan code used to estimate the model is returned as \code{attr(res, "lavaan_code")}.

Since \code{coef()} (used internally to bootstrap the raw path coefficients) does not
return \code{lavaan}'s defined quantities (\verb{:=}), the index of moderated mediation and the
conditional indirect/direct effects are calculated from the bootstrapped raw
coefficients directly in R - exactly analogous to how \code{\link[=run_mediation]{run_mediation()}} reconstructs
indirect effects from bootstrapped \code{a} and \code{b} coefficients.
}
\examples{
\donttest{
# Might the link between depression and self-reported health, through reduced
# physical activity, be stronger for older participants?

set.seed(4321)
res <- run_moderated_mediation(ess_health, X = fltdpr, M = dosprt, W = agea, Y = health,
                               bootstraps = 50)
# NB: bootstraps = 50 only set to reduce running time - should be 1000+

res

attr(res, "CV_coefficients")

# The result can be passed straight to plot_moderated_mediation() for a diagram
# annotated with the estimated coefficients - see ?plot_moderated_mediation
}
}
